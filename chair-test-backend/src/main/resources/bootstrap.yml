spring:
  application:
    name: chairTestSingle
  cloud:
    nacos:
      config:
        server-addr: 00.00.00.00:8848
        namespace: prod
        group: prod
      discovery:
        server-addr: 00.00.00.00:8848
        namespace: prod
        group: prod
  config:
    import:
      - optional:nacos:chairTestSingle.yml